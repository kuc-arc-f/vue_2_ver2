// LibAuth
import firebase from 'firebase'

export default{
    /*
    valid_auth_login: function(){
        firebase.auth().onAuthStateChanged(function(user) {
            if (user) {
                console.log("#Auth-OK");
                return true
            } else {
                console.log('#no-User');
                return false
            }
        })
    },
    */
}
